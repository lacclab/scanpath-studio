# Deployment manifest for the Streamlit Cloud demo. Compatible-release pins
# (`~=`) keep the live app on a known-good minor while still picking up patch
# releases. The library dependency bounds (looser, `>=`) live in pyproject.toml;
# keep both in sync when adding or upgrading a dependency.
streamlit~=1.62.0
pandas~=2.3.3
plotly~=6.5.2
numpy~=2.4.1
# Vertical drift-correction algorithms (alignment.py): k-means + optimizers.
scipy~=1.18.0
pyarrow~=23.0.0
kaleido~=1.2.0
# Optional Streamlit runtime dep: fast event-based file-watching for hot reload
# (not imported by app code; Streamlit polls + nags if missing).
watchdog~=6.0.0
# Drag-to-reorder the trial chip fields (the inline "Edit chips" popover).
streamlit-sortables~=0.3.1
# Rasterized animation export (GIF/MP4). imageio's ffmpeg extra bundles an
# ffmpeg binary, so MP4 needs no system package; Pillow encodes the GIF.
pillow~=12.1.0
imageio[ffmpeg]~=2.37.3
# pandas' .xlsx engine: spreadsheet uploads and MultiplEYE's comprehension
# questions workbook both go through pd.read_excel.
openpyxl~=3.1.5
